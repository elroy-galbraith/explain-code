{
  "title": "The background job queue",
  "subtitle": "Subsystem onboarding · jobs/",
  "slug": "2026-01-12-job-queue-onboarding",
  "gate": [
    {
      "prompt": "The queue guarantees 'at-least-once' delivery. What does that promise actually guarantee?",
      "options": [
        {"text": "The job runs exactly one time — never more than once, never fewer.", "correct": false},
        {"text": "The job may run zero times if a worker is slow enough to never reach it.", "correct": false},
        {"text": "The job runs at least once — possibly more, but never zero times.", "correct": true},
        {"text": "Jobs are processed in the exact order they were originally enqueued, first to last.", "correct": false}
      ],
      "explanation": "At-least-once trades away exactly-once in exchange for never silently losing a job — the cost is that handlers must tolerate an occasional duplicate run."
    },
    {
      "prompt": "Why must job handlers be idempotent?",
      "options": [
        {"text": "It's mainly a performance optimization — idempotent handlers skip redundant work and so finish faster.", "correct": false},
        {"text": "Because a redelivered job may run more than once, so repeating it must stay safe.", "correct": true},
        {"text": "It's what lets a handler safely read from the database while other worker processes run concurrently.", "correct": false},
        {"text": "It only matters for payment jobs, where a duplicate run would double-charge the customer.", "correct": false}
      ],
      "explanation": "Redelivery after a crash or a timeout means 'ran once' isn't guaranteed — only 'ran at least once' is, so re-running the same job must be safe."
    },
    {
      "prompt": "What is the dead-letter queue for?",
      "options": [
        {"text": "An archive of jobs that completed successfully, kept around for later auditing and reporting.", "correct": false},
        {"text": "Holding jobs that exhausted their retries, set aside for a human to inspect.", "correct": true},
        {"text": "A standby backup copy of the main queue, used to take over if it goes down.", "correct": false},
        {"text": "A holding area for jobs scheduled to run at some point in the future.", "correct": false}
      ],
      "explanation": "Retrying a broken job forever just wastes workers; the dead-letter queue is where the system gives up automatically and hands the problem to a person."
    }
  ],
  "sections": [
    {
      "id": "background",
      "heading": "Background",
      "summary": "Some work is too slow to do inside a web request: resizing an uploaded image, generating a PDF report, sending a batch of emails. The job queue lets a request hand that work off to a pool of background workers and return immediately, instead of making the caller wait or timing out.",
      "body": "<p>Without a queue, a request handler that needs to do slow work has two bad options: block the HTTP response until the work finishes (the user stares at a spinner, and the request can time out), or fire off the work and hope it finishes (and lose it entirely if the process restarts mid-task). The job queue is the subsystem that lets that work happen soon and reliably, without making the caller wait.</p><p>Three pieces cooperate: something that <strong>enqueues</strong> jobs (usually a request handler), a <strong>queue</strong> that durably holds them, and a pool of <strong>workers</strong> that pull jobs off the queue and run them.</p>"
    },
    {
      "id": "intuition",
      "heading": "Intuition",
      "summary": "Picture a shared to-do list on a whiteboard. A worker claims a task by writing their name next to it, with a time limit, then erases it once done. If they crash first, the claim expires and someone else can pick the task back up — which is exactly why the same task can occasionally get done twice.",
      "body": "<p>Picture a shared to-do list on a whiteboard. A worker claims a task by writing their name next to it, with a time limit; they do the work, then erase the entry. If they crash before erasing it, the claim eventually expires and someone else can pick the task back up — which is exactly why the same task can occasionally get done twice.</p><div style='display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin:14px 0;font-size:.86rem;'><div style='border:1px solid var(--line);background:var(--card);border-radius:8px;padding:10px 12px;'>Request handler<br><span style='color:var(--muted)'>enqueue()</span></div><div>→</div><div style='border:1px solid var(--line);background:var(--card);border-radius:8px;padding:10px 12px;'>Queue<br><span style='color:var(--muted)'>durable storage</span></div><div>→</div><div style='border:1px solid var(--line);background:var(--card);border-radius:8px;padding:10px 12px;'>Worker pool<br><span style='color:var(--muted)'>lease → run → ack</span></div><div>→</div><div style='border:1px solid var(--bad);background:var(--card);border-radius:8px;padding:10px 12px;'>Dead-letter<br><span style='color:var(--muted)'>gave up</span></div></div><p>That kind of claim — one with a built-in time limit — is called a <strong>lease</strong>; the time limit itself is the <strong>visibility timeout</strong>. Once a worker leases a job, the queue hides it from other workers for that long, betting the worker will finish and acknowledge (<strong>ack</strong>) it first. If the worker crashes, stalls, or is killed before acking, the lease simply expires and the job becomes visible again for any worker to pick up. The queue has no way to tell a worker that's still working from one that died halfway through, so once the timeout passes it always assumes the pessimistic case.</p>"
    },
    {
      "id": "code",
      "heading": "The code",
      "summary": "enqueue() writes a row with a job type and payload. Each worker loops: lease the oldest available job, look up its handler by type, run it, and ack on success. An unhandled exception leaves the job to be retried instead of acked.",
      "body": "<p class='muted'><code>jobs/queue.py</code></p><p>Enqueuing is a single durable write — the caller doesn't wait for the job to run:</p><pre>def enqueue(job_type, payload, max_attempts=5):\n    db.jobs.insert(\n        type=job_type,\n        payload=json.dumps(payload),\n        status='available',\n        attempts=0,\n        max_attempts=max_attempts,\n    )</pre><p class='muted'><code>jobs/worker.py</code></p><p>The worker's poll loop leases one job at a time:</p><pre>VISIBILITY_TIMEOUT = timedelta(minutes=5)\n\ndef worker_loop():\n    while True:\n        job = db.jobs.lease_next(\n            statuses=['available', 'expired_lease'],\n            lease_until=now() + VISIBILITY_TIMEOUT,\n        )\n        if job is None:\n            sleep(1)\n            continue\n        run_job(job)</pre><p>Running a job dispatches to a handler by type, and only acks (marks done) on a clean return:</p><pre>HANDLERS = {'resize_image': resize_image_handler, 'send_email': send_email_handler}\n\ndef run_job(job):\n    handler = HANDLERS[job.type]\n    try:\n        handler(job)\n        db.jobs.ack(job.id)\n    except Exception:\n        db.jobs.mark_failed_attempt(job.id)  # retry logic decides what happens next</pre><p>Notice there's no <code>finally</code>-block ack — acking only on the success path is what makes a crash mid-handler safe: the job just stays leased until the timeout, then becomes available again.</p>"
    },
    {
      "id": "failure-modes",
      "heading": "Failure modes & idempotency",
      "summary": "Retries use exponential backoff so a struggling downstream service isn't hammered immediately; after max_attempts, the job moves to the dead-letter queue instead of retrying forever. Because redelivery is possible, every handler is written to be safe to run twice.",
      "body": "<p class='muted'><code>jobs/worker.py</code></p><p>A failed attempt doesn't retry instantly — the delay grows with each attempt:</p><pre>def mark_failed_attempt(job):\n    job.attempts += 1\n    if job.attempts >= job.max_attempts:\n        move_to_dead_letter(job)\n        return\n    backoff = min(2 ** job.attempts, 300)  # seconds, capped at 5 minutes\n    job.status = 'available'\n    job.available_at = now() + timedelta(seconds=backoff)\n    job.save()</pre><p>The cap matters: uncapped exponential backoff on a job with a generous max_attempts could end up scheduled hours or days out, which is rarely what anyone wants for a job someone is actively waiting on.</p><p>Because a lease can expire and a job be redelivered, every handler needs to tolerate running more than once for the same input. The email handler doesn't track <em>have I sent this before</em> in memory — a second worker process wouldn't know about it — so it hands the email provider an idempotency key instead:</p><p class='muted'><code>jobs/handlers/email.py</code></p><pre>def send_email_handler(job):\n    payload = json.loads(job.payload)\n    email_provider.send(\n        to=payload['to'],\n        template=payload['template'],\n        idempotency_key=job.id,  # provider dedupes on this\n    )</pre><p>If this same job gets redelivered and re-run, the provider recognizes the repeated idempotency key and skips sending a second email — the handler running twice is harmless. Jobs that only touch your own database can get the same property from a unique constraint (an insert-if-not-exists on the same key) instead of relying on a third party.</p>"
    }
  ],
  "quiz": [
    {
      "prompt": "A worker leases a job, then crashes before acking it. What happens to that job?",
      "options": [
        {"text": "The job is lost permanently, since no worker ever acknowledged that it finished.", "correct": false},
        {"text": "It stays leased forever, blocking every other worker until an operator clears it by hand.", "correct": false},
        {"text": "Once its lease expires at the visibility timeout, another worker can claim it.", "correct": true},
        {"text": "It's moved straight to the dead-letter queue for a human to look at.", "correct": false}
      ],
      "explanation": "The visibility timeout is exactly the mechanism that recovers jobs from crashed workers — no manual step needed."
    },
    {
      "prompt": "Why does the worker only ack a job on a clean return from the handler, with no ack in a finally block?",
      "options": [
        {"text": "Acking inside a finally block would add an extra database write and slow the worker down.", "correct": false},
        {"text": "A crash mid-handler leaves the job unacked, so its lease expires and it retries.", "correct": true},
        {"text": "The worker's language doesn't support finally blocks, so a clean return is the only option.", "correct": false},
        {"text": "It cuts down on database writes by acknowledging each job only a single time.", "correct": false}
      ],
      "explanation": "Acking unconditionally would mark a job done even when the handler blew up partway through, losing the work with no retry."
    },
    {
      "prompt": "Why does retry backoff grow with each attempt instead of retrying immediately every time?",
      "options": [
        {"text": "It makes the queue easier to monitor by spreading retry attempts out across the logs.", "correct": false},
        {"text": "Spacing out retries avoids hammering an already-overloaded downstream service and making things worse.", "correct": true},
        {"text": "It's an unavoidable side effect of how leases expire, not something the retry logic chooses.", "correct": false},
        {"text": "It's how the queue guarantees that jobs still run in their original order after failures.", "correct": false}
      ],
      "explanation": "Immediate retries against a struggling dependency tend to prolong the outage; spacing them out gives it room to recover."
    },
    {
      "prompt": "What ultimately happens to a job that keeps failing past max_attempts?",
      "options": [
        {"text": "It keeps retrying forever, spaced out at the maximum backoff interval, never giving up.", "correct": false},
        {"text": "It's silently discarded and deleted from the queue, with no record left behind.", "correct": false},
        {"text": "It moves to the dead-letter queue for a human to inspect.", "correct": true},
        {"text": "It's automatically marked as succeeded so the worker pool can move on to other jobs.", "correct": false}
      ],
      "explanation": "The dead-letter queue is the deliberate stopping point — retrying indefinitely would waste workers on a job that isn't going to succeed on its own."
    },
    {
      "prompt": "Why does the email handler pass the job's id as an idempotency key to the email provider, rather than tracking 'already sent' in the worker's own memory?",
      "options": [
        {"text": "It's faster than doing a database lookup to check whether the email already went out.", "correct": false},
        {"text": "A retry may run on a different worker that never saw the first attempt.", "correct": true},
        {"text": "The email provider rejects any request that doesn't include an idempotency key on it.", "correct": false},
        {"text": "It mainly makes the email arrive at the recipient faster, with fewer delays before sending.", "correct": false}
      ],
      "explanation": "In-memory state doesn't survive a crash and isn't shared across worker processes, so the dedupe has to live somewhere durable and shared — here, the email provider itself."
    }
  ]
}
